<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="c0d3.vitreen.app">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/vitreen_ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/vitreen_ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/VitreenTheme">
        <activity android:name=".activities.adverts.drop.step2.DropAdvert2Activity" />
        <activity android:name=".activities.adverts.drop.step1.DropAdvert1Activity" />
        <activity android:name=".activities.connexion.ConnectionActivity" />
        <activity android:name=".activities.inscription.step_2.Inscription2Activity" />
        <activity android:name=".activities.inscription.step_1.Inscription1Activity" />
        <activity android:name=".activities.MainActivity" android:windowSoftInputMode="adjustNothing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>